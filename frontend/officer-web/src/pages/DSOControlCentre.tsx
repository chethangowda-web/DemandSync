import PortalHome from './PortalHome';

export default () => (
  <PortalHome title="DSO Control Centre"
    next="The 7-stage cycle workflow (monitor, validate, allocate, optimize, authorize, verify, close) arrives in Phase 2. Nothing is shown here until it can be read from the database." />
);
